# Independent browser acceptance

Root used the documented CUA browser surface against the owner's local preview at `http://localhost:5176/`. Checks below describe observed UI behavior, not inferred source behavior. No production scene was saved or overwritten.

## Completed checks

- **Material atlas:** all 19 addable catalog entries rendered actual image thumbnails (each loaded, 240px intrinsic width). Juniper remained source-only. The 1280×720 view showed the compact material tray beside the functioning 3D aquarium.
- **Native W1/W3 import:** selected `blender/frontier-fixtures/20260913-protocol-v1/resolved_changed/frontier-review.json` through the actual file chooser. Ideas showed `Protocol fixture`, Native Blender, recorded revision 2/base 1, requested close-up of `rock-01`, a 2cm fixture displacement and its exact answer observation citation.
- **Protection review:** navigation from Ideas to Arrange showed the blocked `wood-01` position attempt. Native results were explicitly view-only, no Apply control appeared, and the current browser scene remained revision 19. Recorded protected IDs did not replace current browser protection.
- **Evaluation import:** selected the complete `native-fixture-final-20260913/frontier-review.json` through the file chooser. Gallery → Recorded evaluation displayed the separate native series, revision 0 IoU 100.0%, revision 1 IoU 50.3%, delta −49.7 percentage points. The fixture note disclosed intentional rock movement and no real-photo/model claim. Missing SSIM displayed “Not measured,” not zero.
- **Evaluation attachments:** attached the exact local synthetic reference and two actual native render PNGs. The reference hash matched and all three images appeared. At this checkpoint, render attachments were labelled user-associated/unverified; the subsequently added optional render hash requires a targeted swapped-file check.

## Additional completed checks

- **Wrong render rejected:** after importing the updated evaluation wrapper with render hashes, attempted to attach revision 0's PNG to revision 1. The actual page returned “This file does not match the recorded SHA-256. It was not attached.” The owner then colocated that alert with the offending picker after root confirmed its old location was offscreen.
- **390×360:** browser DOM confirmed client width 390, height 360, document scroll width 390. Materials → Inspect 3D opened. The short footer scroll revealed Add and source details; neither was clipped permanently. No horizontal page overflow.
- **390×844:** browser DOM confirmed the requested dimensions and scroll width 390. Actual sample and Add were visible together. Cursor drag visibly changed the sample camera orientation. Closing the sheet restored keyboard focus to the Materials trigger.

## Later independent checks

- **Correct render and mobile evaluation:** root attached the correct revision PNG successfully after the swapped-file rejection. At 390px width, the recorded score table remained locally scrollable and the document did not overflow horizontally. The owner separately verified colocated picker errors after its fix.
- **Version 8 production:** all 19 thumbnail images loaded in root's isolated production tab. Existing user tabs and scene saves were preserved.
- **Version 9 background:** uploaded an original synthetic PNG through the actual chooser, changed photo zoom by keyboard, then removed the image. Dark studio returned. The browser scene stayed revision 19; root did not Save or modify geometry. This tests functionality, not visual matching to a real room.
- **Version 9 camera:** dragged from blank canvas toward the underside. The camera stopped near a horizontal front view without exposing the underside. This complements the owner's actual OrbitControls unit checks; it is not exhaustive camera-path coverage.
- **Version 10 local styling gate:** actual 1280×720 screenshots showed compact radio choices and all three visible slider tracks/thumbs. Original PNG upload, keyboard zoom and Remove succeeded. Local saved scene remained revision 23; no Save was performed. Root cleared its photo fixture and authorized private publication after this visual check.

## Final version 10 production checks

- Reloaded only root's isolated production tab. The saved user scene hydrated to revision 19, 90 × 100 × 56 cm. No scene Save, geometry edit or storage write was performed.
- Actual original-fixture background upload showed all three correctly styled tracks/thumbs. Remove restored Dark studio. The fixture was cleared.
- Imported `evaluation/green-aqua-real/review-with-evaluation.json` through the actual chooser. Ideas showed declared Native Blender revision 2, the real r1 close-up and r4 top-view requests, and cited changes. Browser revision remained 19.
- Gallery displayed 34.4% → 34.4%, a rounded 0.0 percentage-point delta, SSIM “Not measured” and centroid errors 36.3px / 27.3px. The exact small delta remains in the artifact/documentation; the UI does not advertise an improvement.
- Both exact native render PNGs were accepted by their individual recorded hashes and appeared in the correct slots. No original source or held-out frame was attached/rehosted. Native records remained distinct from the browser scene.
- Production browser error/warning log query returned no entries after these interactions.

The actual Green Aqua evaluation review and generated renders were retained only in root's browser session at this checkpoint. A later clean reload for Version 11 cleared these session imports; their durable review/render files remain available for reimport. Core local/mobile/mutation checks above remain separate checkpoint evidence; they were not all repeated on production after the two styling classes changed.

## Accepted real W1 branch import

Root used its other isolated QA tab to import actual run `20260913T045133Z-f468b506/frontier-review.json` on version 10. Ideas displayed native revision 4 and the r1 recorded change: height 0.19 → 0.15 m, citing exact answer observation `obs-a0331ad408f771fa0c40a4d1`; r4 remained Photo requested. Arrange displayed recorded protection for r3, no blocked attempts, and “Native run, view only.” The current browser scene stayed revision 19. This UI check verifies import/display, not geometric fidelity or a cursor-authored native edit. The native snapshot was an explicitly scripted transform fixture, as documented in DELIVERY.md.

## Final version 11 material rendering checks

After the separately requested material pass, root clean-reloaded the isolated production tab. All 19 actual thumbnail images loaded with intrinsic width 240px. The selected Alternanthera sample showed the refined curved blades and colour variation; wood grain, mineral rock variation and restrained sand detail were visible in the scene. Error/warning log query returned no entries. Saved scene revision 19 remained unchanged; no Add, Save or geometry edit was performed. Local final-source checks had exercised the same variants, and the owner separately ran bounds tests across 90 foliage rotation/size cases. This establishes functioning procedural rendering, not scanned-asset fidelity or measured performance on every GPU. Temporary local tab closed; verified production Materials view retained.

Owner's independently performed mutation, undo, stale proposal and save/reload checks are reported separately in release documentation. Root did not attribute those to its own browser interactions.
