# Frontier Site release evidence

**Core version 8 checkpoint.** Later camera/background versions 9–10 are documented in [CAMERA_ROOM_BACKGROUND_RELEASE.md](CAMERA_ROOM_BACKGROUND_RELEASE.md). Use [DELIVERY.md](DELIVERY.md) for the latest verified deployment identity and the separately recorded native experiments. The version 8 identity below is preserved as historical evidence.

13 September 2026. Site owner only; this file supplements the coordinator's DELIVERY.md. Final build and responsive acceptance passed; private publication succeeded at 04:35:34 UTC.

## Implemented

- Nineteen catalog thumbnails use the same catalogDescriptor and makeObject as Add, sequentially rendered by one temporary shared renderer/cache. At most one selected interactive sample, with Reset view and an error Retry. Juniper remains source-only. No commercial reference assets copied.
- Width/depth/height and safe object position join size/turn as bounded sliders with exact numeric fields. Accepted manual geometry/material edits protect the affected object; explicit Release remains available.
- Strict imported review parser, latest-current protection merge, effective value diff, blocked attempts, atomic Apply and single Undo. Successful Apply retains its reviewed snapshot and shows its accepted revision; later scene edits leave it read-only. A genuinely stale unaccepted file cannot apply.
- Evidence requests, immutable local observation/response manifest handoff, explicit inferred/no-citation state, native view-only boundary. Imported declarations do not authenticate a model run.
- Recorded evaluation with honest regressions/missing metrics, exact series identities, reference hash and optional per-revision renderHash checks. Errors/busy states appear beside their photo picker. Native scores are not browser-geometry accuracy.

## Automated checks

- 37 tests passed: existing scene and edge cases, new frontier tests, independent contract-gap suite. Includes no-op protection, substrate rejection, optional render hash, inferred/no-fabricated-citation gate, exact W1 answer, stale/containment, catalog parity and position extrema.
- TypeScript no-emit passed.
- Final full production build passed on the exact source committed as `de65830d5794b29b8ae9c3d0d8f0e960acd98fda`.
- `git diff --check` passed.
- Native parser parity: resolved_changed, resolved_confirmed and request_unresolved fixtures; separate native fixed-camera evaluation wrapper; real recorded run `20260913T042211Z-f9853fc4` (native revision 1, 16 changes and main-stone close-up request).

## Owner browser evidence (localhost, 1280 × 720)

- All 19 real thumbnail images loaded. Interactive Spider Wood preview showed actual geometry and Add remained visible after moving the sample out of the footer.
- Imported native confirmation shows no change, exact request/answer identities and no browser application.
- Browser fixture attempted to move/unlock protected wood and move an unlocked stone. Effective diff showed only stone X −16 → −15 cm. Apply preserved wood byte-for-byte and advanced one revision. One Undo restored all eight original objects. Stale proposal could not reapply.
- Position ArrowRight advanced one revision and automatically protected the stone. Escape from an invalid numeric draft did not advance revision. Explicit release worked; two undos restored the pre-edit geometry/protection.
- A tank width pointer drag advanced one revision; one Undo restored dimensions and every original object.
- Focused Width 61 + Save committed the focused field. Width 9 + Save rejected and focused the inline error without changing canonical revision. Width was restored.
- Manual edit + Save + reload preserved protection and one journal entry. Undo + Save restored the original eight objects and 60 × 40 × 36 cm tank. Final QA local save was revision 23, with the undone edit retained in journal/history. Production storage was never saved or changed.
- Synthetic unresolved request + original local hero render reached “Photo registered locally · waiting for a native revision”, not a fabricated resolution. Downloaded response JSON was inspected: native run revision used as base, correct request/object/view/role/hash, bounded filename. Test response was not sent to a genuine model run.
- Real recorded run displayed declared native provenance, its actual main-stone question and 16 cited changes.
- Native evaluation displayed 100.0% → 50.3% IoU and −49.7 pp, not an invented improvement; missing SSIM showed Not measured.
- Revision 1 PNG in revision 0 attachment slot was rejected by SHA-256; correct revision 0 PNG accepted. After UX fix the hash error was visibly adjacent to the picker.
- Successful proposal displayed “Applied as revision 24 · one undo step”, retained exact reviewed values and attempts, and no erroneous stale alert. Undo changed status to historical/read-only rather than enabling a second Apply.

## Independent responsive gate

Owner's viewport override did not change its browser dimensions and was reset; no mobile proof is claimed from that attempt. Coordinator's independent actual 390 × 360 check passed: no horizontal overflow, sample opens, Add and source details reachable by footer scrolling. Actual 390 × 844 passed: sample and Add visible, drag orbits the sample without scene mutation, Close restores focus to Materials. Minimum-height/internal-scroll fix is implemented. The short panel still requires scrolling; making it taller is optional polish, not a hidden action.

## Publication identity

- Exact source pushed: `de65830d5794b29b8ae9c3d0d8f0e960acd98fda`.
- Version 8: `appgprj_6aa60f318aec8191903686f10de4e48e~appgver_fdbc943253788191835ae6272d6e3ce2`.
- Deployment: `appgdep_6aa62807661c8191abc43e2790b27836`, terminal `succeeded`, no failure message.
- Private URL: https://fishy-3d-studio.banz-joshua.chatgpt.site
- Archive SHA-256: `74e8631a3c6402d03e426f18a20a869e8e66dc15a92a97be150c69475f1f604c`.
- Access readback immediately before publication: owner role, custom access, one allowed account user, zero external visitors, zero workspace groups and zero tenant groups. Private deployment operation used; no sharing changes.
- Existing untracked `site/lib/design.ts` and `site/tsconfig.tsbuildinfo` preserved and excluded from the owned-source commit.

## Limits

Hosted model/voice/image inference is not connected. Genuine native model/evaluation work runs outside the Site and is imported locally. No reconstruction-photo assets are rehosted. Procedural samples remain generic; Behind Glass's textured/organic rendering quality is a future asset/rendering target, not a claimed accomplishment of these interface changes. Existing large Three.js/data bundle warning remains; no new dependencies were added.
