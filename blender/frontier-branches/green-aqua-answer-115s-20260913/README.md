# Separate unscored native answer branch

This branch permits the original three Green Aqua reconstruction frames plus the actual later 01:55 left-foundation close-up, supplied in answer to request-r2-1 for object r1. The fourth frame is another view from the same public video, not an independent photograph. Source rights remain unverified; these frames are local research inputs and are not published to the site.

The original strict two-revision evaluation sequence and its gate remain immutable. This branch has no heldout evaluation score and must not inherit that sequence's scores. The validation policy here identifies a separate input gate; it does not claim that the branch has been evaluated. Original heldout/derived-byte denial hashes remain enforced, and generation must retain OS isolation.

The completed branch used a validated snapshot from an isolated Blender copy with an explicitly **scripted transform fixture**, not a cursor-authored native edit. The user's existing unsaved Blender scene was not changed.

Two actual model calls were made. `20260913T044524Z-04a1e470` was rejected before build for citation scope and unanswered-request resolution errors; its raw output and `rejected-run-analysis.json` are retained. A clarified prompt was used for one bounded fresh call, `20260913T045133Z-f468b506`, which was accepted with no automatic retries.

The accepted result changes r1 height from 0.19m to 0.15m and cites the exact 115-second answer observation. r4 remains requested. Captured protected r3 retains its exact mesh, world matrix and recipe. The model made no override attempt in this accepted run, so no blocked attempt is invented. Separate adversarial fixtures verify the enforcement path. See `accepted-native-verification-with-target-geometry.json` and `followup-execution.json`. This branch remains unscored; it is not a third revision in the completed W2 comparison.
